addUser:
INSERT INTO users(email, password) VALUES (?,?) RETURNING id;

addUserReturning:
INSERT INTO users(id, email, password, created_at) VALUES (?,?, ?, ?) RETURNING id;

findUserByEmail:
SELECT * FROM users WHERE email = ? LIMIT 1;