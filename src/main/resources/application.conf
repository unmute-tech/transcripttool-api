ktor {
    development = true
    deployment {
        port = 8080
        watch = [classes, resources ]
    }
    application {
        modules = [ xyz.reitmaier.transcribe.ApplicationKt.module ]
    }
}